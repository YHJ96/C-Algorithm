function Copy() {
  const handleOnClick = (item: string) => () => {
    navigator.clipboard.writeText(item);
  };
  return (
    <button type="button" onClick={handleOnClick('YHJ')}>
      Copy
    </button>
  );
}

export default Copy;
